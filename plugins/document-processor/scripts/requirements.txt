# Document Processor Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
pypdf>=4.0.0          # PDF text extraction (pure Python)
python-docx>=1.0.0    # DOCX parsing and extraction

# Optional: Enhanced PDF extraction
# Uncomment if you have poppler installed (brew install poppler)
# pdftotext is used via command line, not as a pip package

# Optional: OCR support for scanned PDFs
# Requires: brew install tesseract poppler
# pytesseract>=0.3.10
# pdf2image>=1.16.0
# Pillow>=10.0.0

# Optional: Better DOCX to markdown conversion
# mammoth>=1.6.0
